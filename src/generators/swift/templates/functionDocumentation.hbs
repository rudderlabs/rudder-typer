/**
 {{#if functionDescription}}
  {{functionDescription}}
 {{else}}
  Fires a '{{functionName}}' track call.
 {{/if}}
 
  - Parameters:
    {{#each properties}}
    {{#if advancedKeywordsDoc}}
{{indentDoc advancedKeywordsDoc "    "}}
    {{/if}}
    - {{name}}: {{#if hasEnum}}{{enumName}}{{else}}{{type}}{{/if}}{{#if isRequired}} (required){{else}} (optional){{/if}}{{#if description}} - {{description}}{{/if}}
    {{/each}}
    - options: Event-level options (optional)
 */
