// This file was automatically generated by RudderTyper
// DO NOT EDIT

package com.rudderstack.analytics.ruddertyper

{{#each enums}}
{{#if comment}}
/** {{{comment}}} */
{{/if}}
enum class {{name}}(val value: String) {
  {{#each members}}
  {{name}}({{{value}}}){{#unless @last}},{{/unless}}
  {{/each}}
}

{{/each}}

{{#each classes}}
{{#if comment}}
/** {{{comment}}} */
{{/if}}
data class {{name}}(
  {{#each properties}}
  {{#if comment}}
  /** {{{comment}}} */
  {{/if}}
  val {{name}}: {{type}}{{#if optional}}?{{/if}}{{#unless @last}},{{/unless}}
  {{/each}}
)

{{/each}}

object Analytics {
  {{#each functions}}
  {{#if comment}}
  /** {{{comment}}} */
  {{/if}}
  fun {{name}}(properties: {{propertyType}}) {
    println("""
      |Analytics Event:
      |  Type: {{eventType}}
      {{#if eventName}}|  Name: {{eventName}}{{/if}}
      |  Properties: $properties
    """.trimMargin())
  }

  {{/each}}
}
